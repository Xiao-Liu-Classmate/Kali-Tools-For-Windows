# 安全策略

## 支持版本

仅 **最新 Release（`V*` tag）** 与 `main` 分支获得安全修复。
旧版本 Release 不提供补丁，请升级到最新版。

## 漏洞报告

### 本仓库自身（部署脚本 / GUI / CI）

涉及下列问题欢迎报告：

- 脚本注入、任意文件写入/删除、路径遍历等
- 下载源被篡改或哈希校验绕过
- GUI 中的敏感信息泄露

**原则上请勿公开提交 issue**（避免细节外泄）。推荐途径：

1. 仓库 **Security → Report a vulnerability**（GitHub 私密漏洞报告，优先）
2. 仅当无法使用私密报告通道时，才可提交普通 issue，
   且只描述现象与影响、**不包含可直接利用的细节**，
   由维护者私下联系后补充

### 第三方工具自身（Nmap / Nuclei / Sqlmap 等）

本仓库仅做下载与部署，工具本身的漏洞请按各上游项目
（nmap.org、projectdiscovery/nuclei 等）的安全渠道报告。

## 使用范围

本项目仅用于**已授权**的安全测试与教学环境。
使用者对自身行为及造成的后果负责，详见 README 免责声明。
